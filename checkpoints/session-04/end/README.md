# Message Board checkpoint — Session 4 complete

第4回終了時の基準状態です。Reactが `fetch()` で一覧取得と投稿を行い、APIエラーも画面に表示します。

```bash
npm install
npm run dev
```

この時点ではフロントとバックに `Message` の型が別々にあり、次回Hono RPCで改善します。
