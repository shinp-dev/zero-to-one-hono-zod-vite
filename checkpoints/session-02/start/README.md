# Message Board checkpoint — Session 1 complete

第1回終了時の基準状態です。実装コードは開始時とほぼ同じですが、開発環境の起動・ファイル配置・TypeScriptの型エラー確認まで終えた状態として扱います。

```bash
npm install
npm run dev
```

次は `src/worker/index.ts` に掲示板APIを追加します。
